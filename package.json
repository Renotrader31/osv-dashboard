{
  "name": "osv-dashboard",
  "version": "1.0.0",
  "description": "Options Summary View - Aggregated Options Analytics",
  "scripts": {
    "dev": "vercel dev",
    "build": "echo 'No build step required'",
    "start": "vercel dev"
  },
  "dependencies": {},
  "engines": {
    "node": ">=18"
  }
}
